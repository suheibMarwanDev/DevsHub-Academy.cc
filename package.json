{
  "name": "devshub-academy-credential-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Production-oriented certificate verification and credential management platform.",
  "scripts": {
    "build:static": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "test": "node --test",
    "ci": "npm run check && npm test",
    "smoke": "node scripts/smoke.mjs",
    "backup": "node scripts/export-backup.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
